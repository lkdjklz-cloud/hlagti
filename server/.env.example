# Copy to .env and fill in
DATABASE_URL="file:./dev.db"
PORT=3001
CLIENT_URL=http://localhost:5173
JWT_SECRET=change-me
GUEST_TICKET_TTL_HOURS=12

# Generate with: npx web-push generate-vapid-keys --json
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:dev@hlagti.local

# SMTP (optional — dev falls back to console logging)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=حلاقتي <no-reply@hlagti.local>